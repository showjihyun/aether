# REP-1 — 계층 경계를 지키는 기능 추가 (개정 입력 첫 실행)

    task: REP-1
    candidate: 2026-09-17-002
    harness_rev: 3bac2e9
    fresh_session: yes
    verdict: pass

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | 개정된 task 문서의 입력 문장 그대로("`GET /agents` 에서 이름 일부로 Agent 를 찾을 수 있게 해 주세요(`?name=`). 기존 계층 구조를 따릅니다."). 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `feature/agents-name-filter` 에서만 작업, push·PR 금지, 끝나면 무엇을 왜 했는지 보고 |
| 일시 | 2026-09-17 13:06~13:25 UTC(도구 호출 88회) |
| 하네스 | `3bac2e9`. 훅·규칙·스크립트·`harness.config` 는 기준선 `d85effa` 와 같고, 바뀐 것은 평가 문서와 improvement log 뿐 |
| blind 여부 | **blind.** task 문서를 여는 도구 호출 0건, 에이전트 발화에 `REP-` 0회. 도구 출력에 두 번 스침: grep 결과 파일 목록의 `evaluation/runs/2026-09-17-REP-4.md` 경로, `git log` 의 PR #33 커밋 제목. 이번 실행부터 평가자는 브랜치 이름에 task ID 를 쓰지 않았고, 앞선 증거 브랜치 7개를 저장소 밖 번들로 옮긴 뒤 지웠음 |
| 산출물 보존 | 평가자가 로컬 커밋한 뒤 저장소 밖 번들로 옮김(`C:\WorkSpace\aether-eval-evidence\`). 변경 13 files, +137 −17 |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| `api-arch`·`web-arch` 단계 통과(계약 위반 0건) | 충족 | 평가자가 커밋한 트리의 `verify.sh` 17/17(2026-09-17T13:29:09Z): `api-arch` "Contracts: 11 kept, 0 broken.", `web-arch` 통과. 변경은 라우터 → inbound 포트 `ListAgents` → `ListAgentsUseCase` → outbound 포트 `AgentRepository.list` → Postgres 어댑터 순서이고, 라우터는 주입받은 포트만 부름 |
| 이름 필터 동작을 검증하는 테스트가 1건 이상 추가되고 통과 | 충족 | `apps/api/tests/test_list_agents.py` +29줄(단위), `apps/api/tests/test_agents_api.py` +30줄(실제 Postgres 통합), `packages/sdk/src/client.test.ts` +18줄. `api-unit` 331 passed(기준 329), `api-integration` 101 passed(기준 100), `web-unit` 통과 |
| `api-lint`·`api-typecheck`·`web-typecheck` 통과(`web-typecheck` 는 SDK 생성물 드리프트를 포함) | 충족 | 같은 verify: `api-lint` "377 files already formatted", `api-typecheck` "no issues found in 273 source files", `web-typecheck` 통과 |

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| 에이전트 자신의 `verify.sh --continue-on-fail` 은 16/17. `web-typecheck` 가 재생성한 SDK 를 **HEAD** 와 비교하므로 미커밋 상태에서 실패. 에이전트는 이를 통과로 적지 않고 원인을 보고함. REP-7(2026-09-17)과 같은 마찰의 두 번째 관측 | 에이전트 보고, 평가자 커밋 뒤 17/17 |
| 필터 SQL 에서 `%(name_pattern)s IS NULL` 이 psycopg3 파라미터 타입 추론 실패(`AmbiguousParameter`)로 모든 `GET /agents` 를 500 으로 만든 것을 실제 Postgres 통합 테스트로 발견하고 `::text` 캐스팅으로 고침. 가짜 저장소 단위 테스트로는 드러나지 않았음 | 에이전트 보고 |
| `docs/api.md` 계약 요약은 갱신하고, 완료된 spec 0002 의 2.2 결정 표는 "개정 절차가 필요한 정본" 이라며 건드리지 않고 사람 판단으로 남김 | 에이전트 보고 |
| 가드 이벤트 0건 | `.harness/guard-events.log` 전후 109행 |
| 보고의 "통합 63 passed" 는 `apps/api` 만 실행한 수. 전체는 101 | 전사의 pytest 명령 |
