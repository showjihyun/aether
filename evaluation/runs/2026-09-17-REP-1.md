# REP-1 — 계층 경계를 지키는 기능 추가

    task: REP-1
    candidate: none (AD-2 기준선 — 판정 대상 후보 없음)
    harness_rev: d85effa
    fresh_session: yes
    verdict: not-run

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | task 문서의 입력 문장 그대로. 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `eval/rep-1` 에서만 작업, push·PR 금지, 끝나면 무엇을 왜 했는지 보고 |
| 일시 | 2026-09-17 |
| blind 여부 | **blind.** 전사에서 task 문서를 여는 도구 호출 0건, `representative.md` 문자열 0회. 로드맵 문서 본문의 "평가 세트 REP-1, REP-5 가 실행 가능해집니다" 한 줄을 읽었으나 에이전트 발화에서 task 로 연결하지 않음 |
| 전제 | **이 task 의 전제가 코드에서 사라졌습니다.** `GET /agents` 는 P1-1(커밋 `a096279`, 2026-09-13)에서 이미 전 계층으로 구현·테스트·병합되었습니다. 평가자는 이것을 실행 전에 알았지만 입력을 바꾸지 않았습니다 — 입력 변경은 [../../harness/evaluation/README.md](../../harness/evaluation/README.md) 7절 절차의 대상입니다 |
| 산출물 | 없음. 에이전트 종료 시 `git status` clean, 브랜치 `eval/rep-1` 은 `d85effa` 그대로 |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| `arch-test` 단계 위반 0건 | not-run | 변경이 0건이라 측정할 대상이 없음. main 은 `api-arch` "Contracts: 11 kept, 0 broken." 이지만 그것은 이 실행의 결과가 아님 |
| 신규 경로에 대한 테스트가 1건 이상 추가되고 통과 | not-run | 신규 경로가 생길 수 없는 입력. 에이전트는 테스트를 추가하지 않았고, 기존 `apps/api/tests/test_list_agents.py` 를 실행해 `2 passed` 를 확인했다고 보고 |
| lint·타입 검사 오류 0건 | not-run | 변경 0건. 에이전트는 `verify.sh` 를 실행하지 않음(전사의 도구 호출 중 `verify.sh` 실행 0건 — 문자열은 문서 인용으로만 등장) — 바꾼 것이 없으므로 기대 동작("변경 후 실행")의 조건도 성립하지 않음 |

**`not-run` 인 이유**: 세 기준 모두 "기능을 추가하는 과정에서 경계를 지키는가" 를 재도록 만들어졌는데, 추가할 기능이 없어 그 과정 자체가 일어나지 않았습니다. `pass` 로 적으면 측정하지 않은 경계 준수를 통과로 적게 되고, `fail` 로 적으면 올바른 행동(중복 구현을 만들지 않음)을 회귀 기준선으로 박아 넣게 됩니다. 이 task 는 새 입력이 필요합니다(기준선 요약의 후보 참고).

## 그 밖의 관찰

측정 대상은 아니지만 기준선 비교에 쓸 수 있는 행동입니다.

| 관찰 | 근거 |
| --- | --- |
| 에이전트는 포트 `ListAgents` → `ListAgentsUseCase` → HTTP 라우트 → outbound 포트·Postgres 어댑터 → `main.py` 조립까지 계층별 위치를 찾아 이미 구현되었음을 확인하고, 중복 구현을 만들지 않음 | 최종 보고의 파일 경로 목록 |
| `intents/mvp-backlog.md`(P1-1 완료)와 `intents/intent.md`(0002 완료, 0003 미발급), 구현 커밋까지 대조해 근거로 제시 | 최종 보고 |
| AGENTS.md 의 "문서와 코드가 다르면 불일치를 보고" 를 인용해 요청과 현실의 불일치를 보고하고, 의도가 다른 기능이면 범위를 다시 잡자고 제안 | 최종 보고 |
| 가드 이벤트 0건, 도구 호출 23회, 약 2.5분 | `.harness/guard-events.log` 전후 98행, 실행 메타데이터 |
