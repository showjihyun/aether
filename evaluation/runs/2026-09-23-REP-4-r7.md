# REP-4 — 실동작까지 확인하는 변경 (가드 확장 뒤 blind 실행)

    task: REP-4
    candidate: 2026-09-20-004
    harness_rev: 2426bc5
    fresh_session: yes
    blind: yes
    verdict: fail

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | task 문서의 입력 문장 그대로. 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `feature/stream-idle-comment` 에서만 작업, push·PR 금지, 보고 |
| 일시 | 2026-09-23 13:02:56 ~ 13:25:16 UTC(도구 호출 78회) |
| blind 여부 | **blind.** `.eval-blind.log` 차단 0건이고 접근 시도도 0건 — 전사에 `evaluation/`·`improvement-log/` 를 여는 도구 호출이 없고, 이전 실행을 찾는 `git log --all`·`reflog`·`branch -a` 도 없습니다(`git status` 와 `git branch --show-current` 뿐). 확장한 가드(`2026-09-22-001`, PR #59)를 평가자가 실행 직전에 실제 훅 경로로 확인했습니다 — `improvement-log/2026-09-20-004.yaml` 읽기가 exit 2 로 막혔습니다 |
| 산출물 | 평가자 로컬 커밋 `7fa1ae2`: `events.py` +31 −1, `tests/test_run_events_keepalive.py` 신규(72줄). `infra/docker/out/` 에 근거 파일 3개 |
| 산출물 보존 | 저장소 밖 `C:\WorkSpace\aether-eval-evidence\rep4-r7.bundle` 과 `rep4-r7\`. 로컬 브랜치는 지움 |

## 합격 기준 (2026-09-21 개정, 세 줄)

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| (1) 캡처에 같은 `run_id` 의 실제 이벤트(시작과 `run.finished`)와 keep-alive 가 수신 시각과 함께 있을 것 | **미충족** | `sse-keepalive-timestamps.log` 에 `run_id 09076e85…` 의 `seq 1 run.status`(running, t=0.11s)와 keep-alive 3회는 있으나 **`run.finished` 0건**. 실행자가 이벤트 공백을 만들려고 worker 를 멈추고(`docker compose stop worker`) `RedisEventSink` 로 `seq=1` 만 발행했습니다. 이벤트 경로 자체는 실제입니다(격리 compose 프로젝트 `-p aether-keepalive-demo`, 실제 Redis 스트림, 호스트 `curl`) |
| (2) 이벤트 없는 구간에 keep-alive 2회 이상, 간격 14~16초 | 충족 | t=15.11s · 30.11s · 45.11s — 간격 정확히 15.00초 ×3. 응답 헤더도 `sse-headers.log` 에 `200 OK` · `text/event-stream` 로 남음 |
| (3) 관측 방법 파일이 캡처와 함께 있고 `smoke` 통과 | **미충족(일부)** | 방법을 적은 파일이 없습니다 — 스택 구성·이벤트 발행 경로는 최종 보고에만 있습니다. `smoke` 는 충족: 실행자가 `verify.sh` 를 스스로 돌려 18/18 통과 |

**`fail` 인 이유**: 세 줄 중 두 줄이 미충족입니다. r6 과 비교하면 관측의 질은 같은 수준(격리 스택·실제 Redis·실제 TCP·15.00초)인데, **r6 이 채웠던 두 가지를 r7 은 채우지 못했습니다** — 같은 캡처 안의 종료 이벤트와 방법 파일입니다. r6 실행자는 기준 세 줄이 적힌 후보 파일을 읽었고(`blind: partial`), r7 실행자는 읽지 못했습니다. 그 차이가 그대로 판정 차이로 나타났습니다.

## 다섯 번째 blind 실행까지의 흐름

| 항목 | 09-17 r2 | 09-20 r3 | 09-20 r4 | 09-20 r5 | 09-22 r6 | 09-23 r7 |
| --- | --- | --- | --- | --- | --- | --- |
| blind | 예 | 예 | 예 | 예 | **부분** | 예 |
| 관측 환경 | 로컬 + 가짜 소스 | 로컬 + 실제 Redis·PG | 격리 compose | 격리 compose, 배포 이미지 | 기본 개발 compose | **격리 compose** |
| 같은 Run 의 종료 이벤트 | 없음 | 없음 | 없음 | 있음 | 있음 | 없음 |
| 방법 파일 | 없음 | 없음 | 있음 | 있음 | 있음 | **없음** |
| 4차 기준 판정 | fail | fail | fail | pass(재채점) | pass | **fail** |

blind 실행만 놓고 보면 4차 기준에서 pass 한 실행은 아직 0건입니다. 후보 `2026-09-20-004` 의 회귀 확인("blind 실행 1회에서 pass")은 충족되지 않았습니다.

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| **red 증거 없음.** 구현이 테스트보다 먼저입니다 — `events.py` 편집 13:04:28~13:04:35, 테스트 작성 13:05:23, 첫 테스트 실행 13:05:30. 그 테스트는 처음부터 통과했습니다(13:06:04 `1 passed`) | 전사 시각. improvement log `2026-09-13-002` 재발 |
| 격리 compose 프로젝트(`-p aether-keepalive-demo`)를 쓰고 그 프로젝트만 `down -v` 로 내렸습니다 — r6 이 기본 개발 프로젝트를 지운 사고(`2026-09-22-003`)는 재발하지 않았습니다 | 전사 13:10:48 · 13:19:09 |
| 실행자가 `verify.sh` 전체를 스스로 돌렸습니다(18/18). r6·r5 는 돌리지 않아 평가자가 대신 돌렸습니다 | 최종 보고, 평가자 재확인 18/18 |
| 설계 한계를 스스로 보고했습니다 — 404 판정을 위해 첫 이벤트를 `StreamingResponse` 생성 전에 당기므로, 첫 바이트 이전의 idle 은 keep-alive 가 덮지 못합니다 | 최종 보고 |
| `asyncio.wait_for` 대신 `asyncio.wait` 를 골라 대기 중인 `XREAD` 를 취소하지 않는 구조를 택했습니다 — 앞선 실행들과 같은 결론에 독립적으로 도달했습니다 | `change.patch` |
| 데모 스택용 API 키가 전사에 평문으로 남았습니다. 그 스택은 `down -v` 로 파기되었고 저장소에는 남지 않았습니다 | 전사 13:11:50 이후 |
