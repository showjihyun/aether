# REP-4 — 실동작까지 확인하는 변경 (완화 기준 첫 실행)

    task: REP-4
    candidate: 2026-09-20-003
    harness_rev: bac34ca
    fresh_session: yes
    blind: yes
    verdict: fail

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | 보완된 입력 그대로(keep-alive 15초, 근거를 `infra/docker/out/` 아래 파일로 남겨 제시) |
| 일시 | 2026-09-20 13:2x~13:5x UTC(도구 호출 95회) |
| blind 여부 | **blind, 차단 0건**(평가 문서 접근 시도 없음) |
| 산출물 | 로컬 커밋 `8b78027`: `events.py`, `tests/test_sse_heartbeat.py` 신규. 근거 파일 3개 |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| (1) 캡처가 파일로 있고 응답 200 이며 `run_id` 가 실제 Run 과 같고, 그 Run 의 조회 응답도 파일로 남음 | **미충족(일부)** | 캡처 `sse-heartbeat-raw.log` 존재, `run_id 30f53940…` 는 `POST /agents/{id}/run` 으로 만든 실제 Run. 그러나 `curl -N` 이라 응답 코드가 기록되지 않았고, `GET /runs/{run_id}` 응답 파일이 없음 |
| (2) 이벤트 없는 구간에 keep-alive 2회 이상, 간격 14~16초 | 충족 | 캡처 원문: 13:48:33.568 · 13:48:48.564 · 13:49:03.570 — 간격 14.996s · 14.996s · 15.006s. 운영 기본값 15.0 |
| (3) api 로그 발췌 파일이 있고 `ERROR`·`Traceback` 0건 | **미충족** | 로그 발췌 파일 없음 |
| (4) `run.finished` 뒤 연결 종료가 라이브 캡처 또는 통합 테스트로 확인 | 충족 | 캡처에 같은 `run_id` 의 `run.finished`(succeeded, seq 2)가 있고 그 뒤 스트림 종료. 기존 SSE 통합 테스트 5건도 통과 |
| (5) `smoke` 단계 통과 | 충족 | 평가자가 커밋한 트리의 `verify.sh` 18/18(2026-09-20T13:57:53Z), `api-unit` 354 · `api-integration` 100 passed |

**`fail` 인 이유**: 다섯 줄 중 두 줄이 미충족입니다. 다만 부족한 것은 관측이 아니라 **서류** 입니다.

## 네 번의 실행

| 항목 | 09-17 | 09-20 (1차) | 09-20 (2차) | 09-20 (3차, 이 실행) |
| --- | --- | --- | --- | --- |
| 관측 환경 | 로컬 + 가짜 소스 | 로컬 + 실제 Redis·PG | 격리 compose | **격리 compose, 배포 이미지** |
| 실제 Run | 아니오 | 아니오 | 예(대기 상태) | **예** |
| 같은 Run 의 종료 이벤트 | 없음 | 없음 | 없음 | **있음(succeeded)** |
| keep-alive 간격 | 15.30 / 30.30 | 14.98~15.00 | 15.000 ×5 | 14.996 · 14.996 · 15.006 |
| 근거 파일 | 0 | 0 | 4 | 3(원문·발행 시각·방법 기술) |
| 미충족 줄 | 4 / 6 | 3 / 6 | 3 / 6 | **2 / 5** |

관측의 질은 매 실행 올라갔고, 이번에는 실제 배포 이미지·실제 TCP·운영 기본값으로 시작부터 종료까지를 한 캡처에 담았습니다. 남은 두 줄은 조회 응답과 로그 발췌를 **별도 파일로** 남기라는 요구입니다. 그것들이 없어도 캡처만으로 라이브 경로는 증명됩니다 — 실제 `run_id`, 서버가 찍은 이벤트 시각, 종료 뒤 연결 닫힘이 모두 원문에 있습니다.

후속 제안은 improvement log `2026-09-20-004` 입니다.

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| 시나리오 설계가 정확함: worker 없이 Run 을 `queued` 로 두어 이벤트 공백을 만들고, api 컨테이너 **안에서** 실제 `RedisEventSink` 로 47초 뒤 종료 이벤트를 발행해 공백과 종료를 한 번에 관측 | `sse-heartbeat-evidence.md` |
| 세 번째 실행과 같은 설계 결론(대기 태스크를 취소하지 않음)에 독립적으로 도달 | `change.patch` |
| spec 0002 2.7 에 keep-alive 가 없다는 점을 스스로 지적하고, 승인된 문서라 고치지 않고 판단을 넘김 | 최종 보고 |
| 에이전트가 `verify.sh` 를 실행하지 않아 평가자가 대신 실행 | 최종 보고, `verify.json` |
