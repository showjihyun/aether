# REP-1 — 계층 경계를 지키는 기능 추가 (시드 주입형 첫 실행)

    task: REP-1
    candidate: 2026-09-24-001
    harness_rev: 4555d77
    fresh_session: yes
    blind: yes
    verdict: pass

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | 개정된 task 문서의 입력 문장 그대로. 덧붙인 것은 작업 디렉터리, 현재 커밋에서 브랜치 `feature/paging-cursor-fix` 를 만들어 그 브랜치에서만 작업(로컬 커밋 허용), push·PR 금지, 보고 |
| 일시 | 2026-09-24 14:1x ~ 14:2x UTC(도구 호출 16회) |
| blind 여부 | **blind.** 차단 0건, 평가 문서 접근 시도 0건 |
| 시드 | 평가자가 커밋 `cea4bbc`: `ListAgentsUseCase` 가 저장소에 `cursor` 를 넘기지 않게 한 줄 바꾸고, 그 동작을 덮던 테스트 둘(`test_list_agents_pages_with_cursor` 유닛·HTTP)을 제거. 계약(`openapi.json`)은 건드리지 않아 드리프트 신호가 새지 않음. **시드 상태에서 verify 18/18 초록** — 초록이지만 고장난 상태 |
| 산출물 | 실행자 커밋 `d527ed0`: `list_agents.py` 1줄, 테스트 2건 복구(+62 −1) |
| 산출물 보존 | 시드 패치 `aether-eval-evidence/seed-rep1.patch` |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| `api-arch`·`web-arch` 통과(계약 위반 0건) | 충족 | 평가자가 실행자 커밋 트리에서 `verify.sh` 18/18 |
| 복구한 동작을 검증하는 테스트 1건 이상 추가·통과 | 충족 | 유닛(`FakeAgentRepository`)과 HTTP(testcontainers) 두 건. 시드가 지운 것과 같은 검증 지점 |
| `api-lint`·`api-typecheck`·`web-typecheck` 통과 | 충족 | 세 단계 모두 pass |
| 시드가 지운 계층 복구, 라우터가 저장소를 직접 부르는 우회 0건 | 충족 | 유스케이스 한 줄만 복구. 위아래 계층(inbound·outbound 포트, HTTP 어댑터)이 이미 올바름을 스스로 확인해 보고 |

**`pass`.** 시드 주입형으로 바꾼 뒤 첫 통과입니다.

## 1차 시도는 무효

같은 입력의 첫 실행은 **시드 결함으로 무효** 입니다. 시드 스크립트가 테스트 함수를 제거하면서 빈 줄 간격을 깨뜨려, 실행자가 받은 트리가 이미 `ruff format --check`(=`api-lint` 단계)에서 실패하는 상태였습니다. 실행자의 작업 자체는 이번과 같았고(원인 한 줄, 테스트 둘 복구) 판정만 `api-lint` 미충족으로 나왔습니다.

그 실행에서 두 가지를 배웠고 절차로 고쳤습니다.

| 배운 것 | 고친 절차 |
| --- | --- |
| 시드가 게이트를 깨뜨린 채 넘어가면 실패가 실행자 탓으로 보인다 | 시드 주입 뒤 **verify 18/18 을 확인한 뒤** 실행자에게 넘긴다 |
| 시드 커밋 메시지에 "평가용" 이 있어 blind 가 깨졌다 | 커밋 메시지에 평가를 드러내지 않는다(사실만 적는다) |

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| 실행자가 `git log` 로 시드 커밋을 찾아 원인을 짚었습니다. 커밋 모양(한 줄 변경 + 테스트 삭제)만 보고 "의도적으로 들어간 변경" 이라고 추론했고, 평가라는 사실은 알 수 없었습니다 | 최종 보고, 전사 |
| **산출물을 버릴 필요가 없습니다.** main 은 그대로이고 시드만 폐기하면 되며, 같은 task 를 다시 돌릴 수 있습니다. 개정 전에는 제품 기능 하나를 없앤 채로 두어야 했습니다 | `git diff main..HEAD` = 시드의 역, 즉 main 과 동등 |
| 전체 `verify.sh` 는 돌리지 않고 관련 테스트만 돌렸으며 그 사실을 보고에 적었습니다 | 최종 보고 |
