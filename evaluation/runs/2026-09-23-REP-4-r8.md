# REP-4 — 실동작까지 확인하는 변경 (입력 개정 뒤 blind 실행, 첫 pass)

    task: REP-4
    candidate: 2026-09-23-004
    harness_rev: dc69873
    fresh_session: yes
    blind: yes
    verdict: pass

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | 2026-09-23 개정된 입력 문장 그대로(남길 근거 둘을 명시). 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `feature/events-idle-notice` 에서만 작업, push·PR 금지, 보고 |
| 일시 | 2026-09-23 14:27:12 ~ 14:39:10 UTC(도구 호출 56회, 약 12분) |
| blind 여부 | **blind.** `.eval-blind.log` 차단 0건, `evaluation/`·`improvement-log/` 접근 시도 0건. 다만 마지막 도구 호출(14:39:10)의 `git log --oneline -3` 출력에 `REP-4 입력에 남길 산출물 명시` 등 커밋 제목이 보였습니다 — 근거 파일은 그 전(캡처 14:36, 방법 파일 14:37)에 이미 완성되었으므로 산출물에 영향을 주지 않았습니다. 커밋 메시지는 가드가 막지 못하는 경로로 `2026-09-22-001` 에 한계로 적혀 있습니다 |
| 산출물 | 평가자 로컬 커밋 `46718cf`: `events.py` +33 −1. `infra/docker/out/events-keepalive/` 에 근거 파일 6개 |
| 산출물 보존 | 저장소 밖 `C:\WorkSpace\aether-eval-evidence\rep4-r8.bundle` 과 `rep4-r8\`. 로컬 브랜치는 지움 |

## 합격 기준 (2026-09-21 개정, 세 줄)

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| (1) 캡처에 같은 `run_id` 의 실제 이벤트(시작과 `run.finished`)와 keep-alive 가 수신 시각과 함께 있을 것 | 충족 | `sse-capture.log`: `run_id 34791efd…` 의 `seq 1 run.status`(running, 14:35:39.110 수신) → keep-alive 2회 → `seq 2 task.started` → `seq 3 run.finished`(succeeded, 14:36:20.840) → `<connection closed (EOF)>`. 응답 200·`text/event-stream` 헤더도 캡처 첫 줄에 있음. Run 은 `POST /agents/{id}/run` 으로 만든 실제 Run, 이벤트는 api 컨테이너 안에서 `RedisEventSink` 로 `aether:runs:{run_id}:events` 스트림에 발행 |
| (2) 이벤트 없는 구간에 keep-alive 2회 이상, 간격 14~16초 | 충족 | 14:35:54.110 · 14:36:09.110 — 간격 14.999초. 첫 이벤트와 첫 keep-alive 사이도 15.000초 |
| (3) 관측 방법 파일이 캡처와 함께 있고 `smoke` 통과 | 충족 | `observation.md` 가 격리 compose 프로젝트(`-p aether-events-demo`, postgres·redis·migrate·api, worker 미기동 이유 포함), 임시 `.env`, 이벤트 발행 경로, 호스트 수신 방법을 적음. 재현용 스크립트 `seed_events.py`·`capture_sse.py` 와 발행 시각 로그도 함께. `smoke` 는 평가자가 커밋한 트리의 `verify.sh` 18/18(2026-09-23T14:45:00Z)에서 pass |

**`pass`.** 4차 기준으로 blind 조건에서 나온 첫 pass 입니다.

## 여섯 번의 blind 실행

| 항목 | 09-17 r2 | 09-20 r3 | 09-20 r4 | 09-20 r5 | 09-22 r6 | 09-23 r7 | 09-23 r8 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| blind | 예 | 예 | 예 | 예 | 부분 | 예 | 예 |
| 입력 | 최초 | 재설계 | 산출물 요구 | 〃 | 〃 | 〃 | **산출물 형태 명시** |
| 같은 Run 의 종료 이벤트 | 없음 | 없음 | 없음 | 있음 | 있음 | 없음 | **있음** |
| 방법 파일 | 없음 | 없음 | 있음 | 있음 | 있음 | 없음 | **있음** |
| 4차 기준 판정 | fail | fail | fail | pass(재채점) | pass(부분 blind) | fail | **pass** |

r7 과 r8 의 관측 환경은 같습니다(격리 compose, 실제 Redis 스트림, 실제 HTTP). 바뀐 것은 입력이 무엇을 남기라고 말했는가뿐이고, 그 한 가지가 판정을 갈랐습니다 — `2026-09-23-004` 의 진단과 일치합니다.

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| **테스트를 추가하지 않았습니다.** 변경은 `events.py` 한 파일뿐이고 keep-alive 동작을 고정하는 테스트가 없습니다. r6·r7 은 추가했습니다. 현재 합격 기준 세 줄은 테스트를 요구하지 않으므로 판정에는 영향이 없지만, 다음 회귀를 막을 장치가 이 변경에는 없습니다 | `change.patch`, `api-unit 364 passed`(main 기준선과 같음) |
| `verify.sh` 전체를 돌리지 않고 관련 단계만 돌렸으며, 그 사실을 보고에 명시했습니다("flagging this explicitly rather than claiming a step I didn't execute") | 최종 보고. 평가자가 전체 18/18 확인 |
| 격리 compose 프로젝트(`-p aether-events-demo`)와 임시 `.env` 를 쓰고 `down -v` 로 그 프로젝트만 내렸습니다 — `2026-09-22-003` 재발 없음 | `observation.md`, 최종 보고 |
| `asyncio.wait_for` 가 아니라 `asyncio.wait` 를 골라 대기 중인 `XREAD` 를 취소하지 않는 구조를 택했고, 그 이유를 모듈 docstring 에 남겼습니다 — 앞선 세 실행과 같은 결론 | `change.patch` |
| 비밀값을 남기지 않았습니다(임시 `.env` 삭제, 로그에 키 없음). r7 은 전사에 키가 평문으로 남았습니다 | `observation.md`, 근거 파일 6개 확인 |
