# REP-4 — 실동작까지 확인하는 변경 (입력 보완 뒤 실행)

    task: REP-4
    candidate: 2026-09-20-002
    harness_rev: ee20b7a
    fresh_session: yes
    blind: yes
    verdict: fail

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | 보완된 task 문서의 입력 문장 그대로(keep-alive 15초, 근거를 `infra/docker/out/` 아래 **파일로 남겨** 제시). 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `feature/stream-heartbeat` 에서만 작업, push·PR 금지, 보고 |
| 일시 | 2026-09-20 09:2x~12:5x UTC. 도중 한 번 무응답으로 중단되어 평가자가 두 번 이어 붙임(작업 지시는 "이어서 마무리" 뿐) |
| blind 여부 | **blind, 차단 1건.** 09:40:30 에 `grep -rn ... evaluation/ intents/ improvement-log/` 가 훅에 막힘(`.eval-blind.log`). 그 뒤 평가 문서 재시도 없음 |
| 산출물 | 브랜치 로컬 커밋 `84f2399`: `events.py`, `tests/test_events_keepalive.py` 신규. `infra/docker/out/` 에 근거 파일 4개 |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| (1) 캡처가 `infra/docker/out/` 에 있고 같은 `run_id` 의 `run.status` 와 `run.finished` 가 함께 있음 | **미충족** | `sse-heartbeat-capture.raw.txt` 에 `run.status`(running, run_id `7d9c7036…`)와 keep-alive 5회는 있으나 `run.finished` 0건. 실행자가 이벤트 없는 구간을 만들기 위해 **worker 를 의도적으로 띄우지 않음**(`sse-heartbeat-evidence-notes.txt`) |
| (2) 그 `run_id` 의 조회 응답이 종결 상태이고 파일로 남음 | **미충족** | 해당 파일 없음. worker 가 없어 Run 은 계속 대기 상태 |
| (3) 이벤트 없는 구간에 keep-alive 2회 이상, 간격 14~16초 | 충족 | 캡처 원문: `t=15.031 / 30.031 / 45.031 / 60.031 / 75.031` — 간격 정확히 15.000초, 75초 이상 관측. 응답 헤더도 캡처 첫 줄에 `status=200 content-type=text/event-stream` |
| (4) 관측 구간의 api 로그 발췌가 파일로 있고 `ERROR`·`Traceback` 0건 | **미충족** | 로그 발췌 파일 없음. 메모 파일이 "서버 로그에 예외 없음" 이라고 서술할 뿐 원문이 없음 |
| (5) `run.finished` 뒤 연결 종료 유지 | 충족(테스트로만) | 신규 단위 테스트 3건 중 하나가 keep-alive 뒤 이벤트 순서 보존을 확인. 라이브 캡처에는 종결이 없음 |
| (6) `smoke` 단계 통과 | 충족 | `verify.sh` 18/18, `api-integration` 100 passed |

**`fail` 인 이유**: 여섯 줄 중 세 줄이 미충족입니다.

## 세 번의 실행이 보여 주는 것

| 항목 | 09-17 | 09-20 (1차) | 09-20 (2차, 이 실행) |
| --- | --- | --- | --- |
| 관측 환경 | 로컬 uvicorn + 가짜 이벤트 소스 | 로컬 uvicorn + 실제 Redis·Postgres | **격리 docker compose 스택**(postgres·redis·migrate·api) |
| 근거 파일 | 없음 | 없음 | **4개**(캡처 원문·단절 출력·절차 메모·캡처 스크립트) |
| 간격 | 15.30 / 30.30 | 14.98 / 15.00 / 15.00 | 15.000 ×5, 75초 이상 |
| 미충족 줄 | 4 | 3 | 3 |

기준을 조일 때마다 관측의 질이 한 단계씩 올라갔습니다. 남은 미충족은 성의 문제가 아니라 **기준끼리의 충돌** 로 보입니다. keep-alive 를 보려면 이벤트가 없어야 하고, `run.finished` 를 보려면 worker 가 돌아야 합니다. 둘을 한 캡처에 담으려면 스트림을 연 채 worker 를 나중에 띄우는 2단계 시나리오가 필요한데, 입력에는 그 요구가 없고 task 의 목적("테스트 통과와 실제 동작을 구분해 관찰")에도 필수가 아닙니다.

후속 제안은 improvement log `2026-09-20-003` 입니다.

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| **blind 훅의 첫 실제 차단.** 평가 문서를 훑으려는 grep 이 막혔고 이후 재시도가 없었습니다 | `.eval-blind.log` 1줄 |
| **훅이 막지 못한 누출 경로**: 실행자가 `git reflog` 로 같은 과제의 이전 실행 브랜치(`feature/sse-keepalive`, 커밋 `5cc177a "평가 증거, 병합 대상 아님"`)를 발견하고, 설계가 자기 결론과 같은지 대조했다고 보고했습니다. 증거 브랜치를 지워도 reflog 와 도달 불가 객체는 남습니다 | 에이전트 보고, 평가자 확인(`git reflog` 에 관련 항목 19건) |
| 설계 판단은 세 실행 모두 같은 결론(대기 태스크를 취소하지 않고 재사용)에 도달했고, 이번에는 그 이유를 실제 재현으로 확인 | 에이전트 보고, `events.py` |
| 뒷정리: 띄운 컨테이너와 임시 키 파일을 스스로 정리 | `docker ps -a` 에 aether 컨테이너 0건 |
