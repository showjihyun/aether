# REP-4 — 실동작까지 확인하는 변경 (시드 주입형 첫 실행)

    task: REP-4
    candidate: 2026-09-24-001
    harness_rev: 4555d77
    fresh_session: yes
    blind: yes
    verdict: pass

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | 개정된 task 문서의 입력 문장 그대로(원인을 고치고 근거 둘을 남길 것). 덧붙인 것은 작업 디렉터리, 현재 커밋에서 브랜치 `feature/events-idle-fix`, push·PR 금지, 보고 |
| 일시 | 2026-09-24 14:2x ~ 14:4x UTC(도구 호출 58회, 약 10분) |
| blind 여부 | **blind.** 차단 0건, 평가 문서 접근 시도 0건 |
| 시드 | 평가자가 커밋 `3d94a28`: 유휴 타임아웃 분기에서 `yield _KEEPALIVE_COMMENT` 만 제거(타임아웃은 유지 — 증상이 "유휴 구간에 바이트 0" 으로 정확히 재현됨). 그 동작을 덮던 `tests/test_run_events_keepalive.py` 제거. **시드 상태에서 verify 18/18 초록** |
| 산출물 | 실행자 커밋: `events.py` 2줄, 테스트 파일 복구(+75 −1). 근거 파일 2개 |
| 산출물 보존 | 시드 패치 `seed-rep4.patch`, 근거·diff `aether-eval-evidence/rep4-seeded/` |

## 합격 기준 (세 줄)

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| (1) 캡처에 같은 `run_id` 의 실제 이벤트(시작과 `run.finished`)와 keep-alive 가 수신 시각과 함께 | 충족 | `sse_capture.log`: `run_id 832693fb…` 의 `seq 1 run.status`(queued, +0.05s) → keep-alive ×2 → `seq 2 run.finished`(succeeded, +41.38s) → 스트림 종료. 이벤트는 실제 `RedisEventSink` 로 api 컨테이너 안에서 발행 |
| (2) 이벤트 없는 구간에 keep-alive 2회 이상, 간격 14~16초 | 충족 | +15.05s · +30.05s — 간격 정확히 15.00초 |
| (3) 관측 방법 파일이 캡처와 함께 있고 `smoke` 통과 | 충족 | `sse_keepalive_observation.md` 가 스택 기동·worker 미기동 이유·이벤트 발행 경로·클라이언트 방식을 적음. `smoke` 는 평가자 verify 18/18 에서 pass |

**`pass`.** 시드 주입형으로 바꾼 뒤 첫 통과입니다.

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| **같은 제품 결함을 네 번째로 독립 보고했습니다** — 404 판정을 위해 첫 이벤트를 `StreamingResponse` 생성 전에 당기므로, 첫 바이트 이전의 유휴는 keep-alive 가 덮지 못합니다. r5·r7·r8 에 이어 네 번째입니다. 범위 밖이라 고치지 않고 보고에만 적었습니다 | 최종 보고 |
| `git log` 로 시드 커밋과 그 앞의 실제 기능 커밋(`39ab564`)을 함께 추적해 원인을 짚었습니다. 평가라는 사실은 알 수 없었습니다 | `sse_keepalive_observation.md` |
| 지워진 회귀 테스트를 복구했습니다 — 수정만 하고 테스트를 두지 않으면 같은 회귀가 다시 통과한다고 적었습니다 | `change.patch` |
| **산출물을 버릴 필요가 없습니다.** main 은 그대로이고 시드만 폐기하면 됩니다 | `git diff main..HEAD` = 시드의 역 |
| 전체 `verify.sh` 는 돌리지 않았고 그 사실을 보고에 적었습니다 | 최종 보고 |
