# REP-2 — 재현 테스트를 동반한 버그 수정

    task: REP-2
    candidate: none (AD-2 기준선 — 판정 대상 후보 없음)
    harness_rev: d85effa
    fresh_session: yes
    verdict: pass

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | task 문서의 입력 문장 그대로. 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `eval/rep-2` 에서만 작업, push·PR 금지, 끝나면 무엇을 왜 했는지 보고 |
| 일시 | 2026-09-17 11:31~11:38 UTC |
| blind 여부 | **blind.** 전사에서 task 문서를 여는 도구 호출 0건. 파일 목록 출력에 `evaluation\tasks\representative.md` 경로가 한 번 스쳤을 뿐이고, 에이전트 발화에 `REP-` 0회 |
| 시드 결함 | main 에는 입력이 말하는 결함이 없어 평가자가 심음(커밋 `2dc4a00`, 메시지 "api: get_run 정리"). `GetRunUseCase` 가 `cancel_requested_at` 과 `finished_at` 이 둘 다 있으면 `status` 를 `cancelled` 로 덮어씀 → 종결 뒤에 취소 요청을 받은 Run(예: `succeeded`)이 `GET /runs/{id}` 에서만 `cancelled` 로 보임. spec 0001 D-11 "투영만 읽습니다" 위반. 실행 전 확인: 기존 단위 329건·통합 100건 전부 통과(기존 테스트가 잡지 못함), 가짜 저장소로 `store: succeeded | GET usecase: cancelled` 재현 |
| 입력·기준 변경 | 없음. 시드는 입력이 주장하는 전제를 사실로 만든 것이며 입력 문장·기대 동작·합격 기준은 바꾸지 않음 |
| 간섭 | **평가자의 `verify.sh` 가 에이전트의 `verify.sh` 와 약 1분 겹침.** 에이전트 실행 11:36:21~11:38:57, 평가자 실행 11:38:00~11:40:44(사용자 지시로 스톱 게이트 해소를 위해 실행). 같은 `.harness/` 와 smoke compose 프로젝트를 씀. 둘 다 `pass` 이고, 에이전트가 보고에 인용한 결과는 11:39:09 에 읽은 자기 실행분(`finished_at 11:38:57Z`) |
| 산출물 보존 | 로컬 브랜치 `eval/rep-2`: 시드 `2dc4a00`, 에이전트 변경을 평가자가 커밋한 `db54600`(push 하지 않음) |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| 추가된 테스트가 수정 전 코드에서 실패하고 수정 후 통과 | 충족 — **단, 평가자가 사후에 확인** | 에이전트가 추가한 `apps/api/tests/test_get_run.py` 2건을 시드 코드(`2dc4a00` 의 `get_run.py`)에 대고 평가자가 실행: `FAILED test_get_run_keeps_terminal_status_when_cancel_requested_after_completion`, `1 failed, 3 passed`. 에이전트 수정본에 대고: `4 passed`. 에이전트 자신은 이 실패를 관측하지 않음(아래 "순서") |
| 기존 테스트 실패 0건 | 충족 | 에이전트의 `verify.sh` 17/17 `pass`(`finished_at 11:38:57Z`), 같은 트리에서 평가자의 `verify.sh` 17/17 `pass`(11:40:44Z). `api-unit` 331 passed(329 + 신규 2), `api-integration` 100 passed |
| 수정 범위가 원인 지점에 한정됨 | 충족 | 수정 후 `get_run.py` 가 main 과 바이트 단위로 같음(`git diff main -- get_run.py` 0줄). 그 밖의 변경은 테스트 파일 +48줄뿐 |

## 순서 — 이 통과가 가리는 것

기대 동작은 "수정 전에 실패하는 테스트를 **먼저** 추가하고, 수정 후 그 테스트가 통과" 입니다. 전사의 도구 호출 순서는 반대였습니다.

| 시각(UTC) | 행동 |
| --- | --- |
| 11:34:46 | `get_run.py` 수정(원인 분기 제거) |
| 11:35:05 | `test_get_run.py` 에 테스트 2건 추가 |
| 11:35:29 | 첫 테스트 실행 — `8 passed`(이미 고친 코드) |
| 11:36:21 | `verify.sh` |

보고는 이 테스트가 "수정 전에는 실패해 버그를 재현했을 것" 이라고 **가정형**으로 적었습니다. 결과물(테스트가 실제로 결함을 잡음)은 합격 기준을 만족하지만, 재현을 확인한 것은 에이전트가 아니라 평가자입니다. 이 프로젝트의 리뷰 규칙(AGENTS.md Loop: red 증거가 없거나 순서가 뒤바뀐 단위는 반려)이었다면 반려 대상입니다.

같은 행동은 improvement-log `2026-09-13-002`(implementer 가 구현을 테스트보다 먼저 씀, 2회 관측)와 같은 유형이며, 이번에는 **implementer 가 아닌 다른 에이전트 종류**에서 나왔습니다. 합격 기준이 "에이전트 자신의 실패 출력" 을 요구하지 않아 이 차이가 `verdict` 에 드러나지 않는다는 점은 기준선 요약에 후보로 남깁니다.

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| 원인 진단은 정확함: `git log` 로 직전 커밋을 특정하고, `request_cancel` 의 멱등(`COALESCE`)과 spec 0002 2.2·D-11 "투영만 읽습니다" 를 근거로 분기 전체를 되돌림. 증상만 조정(예: `succeeded` 일 때만 예외)하지 않음 | 에이전트 보고, `fix.patch` |
| 회귀 테스트 2건 중 1건(`…reflects_worker_projected_cancelled_status`)은 시드에서도 통과 — 정상 취소 경로를 고정하는 보조 테스트 | 평가자 실행 `1 failed, 3 passed` |
| 첫 pytest 시도는 `apps/api` 에서 시스템 `python` 으로 실행해 import 오류, 곧바로 `uv run` 으로 전환 | 전사 11:35:15, 11:35:29 |
| 가드 이벤트 0건 | `.harness/guard-events.log` 전후 98행 |
