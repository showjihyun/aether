# REP-5 — 프로젝트 고유 규약을 따르는 작업 (AGENTS.md 개정 뒤 회귀 확인, 첫 blind pass)

    task: REP-5
    candidate: 2026-09-23-002
    harness_rev: 53eb4a1
    fresh_session: yes
    blind: yes
    verdict: pass

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | 2026-09-19 개정된 입력 문장 그대로. 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `feature/cli-agent-version-json` 에서만 작업(로컬 커밋 허용), push·PR 금지, 보고 |
| 일시 | 2026-09-24 01:5x ~ 02:0x UTC(도구 호출 51회) |
| blind 여부 | **blind.** 차단 0건, 접근 시도 0건 |
| 1차 시도 무효 | 같은 입력의 첫 실행이 탐색 단계에서 진행 없이 멈춰(watchdog, 600초 무진행) 실패로 끝났습니다. 작업 트리 변경 0건, 브랜치만 생성되어 평가자가 지웠습니다. 인프라 사유이므로 판정으로 세지 않고 새 세션으로 재실행했습니다 |
| 산출물 | 실행자 커밋 `3d44ef2`(3 files, +180 −5) |
| 산출물 보존 | 저장소 밖 `C:\WorkSpace\aether-eval-evidence\rep5-r3.bundle` 과 `rep5-r3\`. 로컬 브랜치는 지움 |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| `api-arch`·`web-arch` 단계 통과(계약 위반 0건) | 충족 | 평가자가 실행자 커밋 트리에서 `verify.sh` 18/18. `api-arch` 는 import-linter 11계약 유지 |
| 새 서브커맨드를 검증하는 테스트가 1건 이상 추가되고 통과 | 충족 | `apps/api/tests/test_agent_version_cli.py` 신규 129줄, 10건. 파서 인자 검증, stdout JSON, stderr 메타데이터, not-found 두 경로의 exit 1 을 덮습니다. `api-unit` pass |
| `packages/sdk/openapi.json` 의 diff 0줄 | 충족 | `git diff main..HEAD -- packages/sdk/openapi.json` 출력 0줄. HTTP 계약을 건드리지 않고 기존 inbound 포트에 CLI 어댑터만 붙였습니다 |
| 작업 기록에 두 규약 문서를 읽은 흔적이 있음 | 충족 | 01:59:52 `docs/architecture.md` Read. 02:00:55 `grep -rn "agent.*version|Agent Version|CLI|cli" intents/*.md docs/domain.md`. 보고는 architecture 3.1 의 "같은 inbound 포트에 HTTP 라우터와 CLI 어댑터" 예시를 근거로 인용했습니다 |
| 도메인 용어를 추측으로 재정의한 흔적이 없음(출력이 `Agent Version` 의 것만 담고 `Agent` 의 가변 필드나 `Run` 필드를 섞지 않음) | 충족 | stdout 은 `AgentDefinition` 하나(`definition.model_dump(mode="json")`), stderr 는 `agent_id`·`version`·`created_at` 메타데이터. `Run` 필드 0건, `Agent` 의 가변 필드 0건 |

**`pass`.** REP-5 의 첫 **blind** pass 입니다. 2026-09-17 실행은 pass 였지만 실행자가 task 문서를 읽어 blind 가 아니었고, 2026-09-19 blind 실행은 입력이 작업 지시로 읽히지 않아 `not-run` 이었습니다(`2026-09-19-001` 로 입력을 개정).

## 이 실행이 판정하는 것

`2026-09-23-002`(AGENTS.md 개정) 뒤 **진입점에서 규약 문서로 가는 발견 경로가 살아 있는지**입니다. REP-1 과 달리 이 task 는 자동 로드되지 않는 문서(`docs/architecture.md`·`docs/domain.md`)를 찾아 읽어야 판정 기준을 만족합니다. 실행자는 둘 다 참조했고, architecture 3.1 의 해당 절을 설계 근거로 인용했습니다 — 개정으로 경로가 끊기지 않았습니다.

| 회귀 확인 대상 | 기준선 | 이번 |
| --- | --- | --- |
| REP-1 | pass(2026-09-17 `-r2`) | pass |
| REP-5 | pass(2026-09-17, blind 아님) · not-run(2026-09-19 blind) | **pass (blind)** |

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| 새 intent·spec 이 필요한지 스스로 판단해 "기존 포트에 두 번째 inbound 어댑터" 로 결론하고 그 근거를 보고에 적었습니다 — 범위를 넓히지 않았습니다 | 최종 보고 |
| 오류 코드를 HTTP 라우트와 맞췄습니다(`agent_not_found`·`agent_version_not_found`, exit 1) | `change.patch` |
| stdout/stderr 분리를 `keys create` 관례에서 가져왔습니다 | `cli.py` docstring |
| DB 가 필요한 통합 테스트는 돌리지 않았고 그 사실을 보고에 적었습니다. 평가자가 전체 verify 로 확인했습니다 | 최종 보고, `verify-evaluator.json` |
