# REP-1 — 계층 경계를 지키는 기능 추가 (AGENTS.md 개정 뒤 회귀 확인)

    task: REP-1
    candidate: 2026-09-23-002
    harness_rev: 53eb4a1
    fresh_session: yes
    blind: yes
    verdict: pass

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | task 문서의 입력 문장 그대로. 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `feature/agents-name-filter` 에서만 작업(로컬 커밋 허용), push·PR 금지, 보고 |
| 일시 | 2026-09-24 00:3x ~ 00:5x UTC(도구 호출 66회) |
| blind 여부 | **blind.** 차단 0건, 접근 시도 0건 |
| 산출물 | 실행자 커밋 `896883d`(12 files, +125 −14) |
| 산출물 보존 | 저장소 밖 `C:\WorkSpace\aether-eval-evidence\rep1-r3.bundle` 과 `rep1-r3\`. 로컬 브랜치는 지움 |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| `api-arch`·`web-arch` 단계 통과(계약 위반 0건) | 충족 | 평가자가 실행자 커밋 트리에서 `verify.sh` 18/18. `api-arch`(import-linter 11계약)·`web-arch` pass |
| 이름 필터 동작을 검증하는 테스트가 1건 이상 추가되고 통과 | 충족 | `test_list_agents.py::test_list_agents_filters_by_name_substring_case_insensitive`(유스케이스), `test_agents_api.py` 2건(HTTP 일치·불일치), `packages/sdk/src/client.test.ts` 1건(쿼리 인코딩) |
| `api-lint`·`api-typecheck`·`web-typecheck` 통과 | 충족 | 세 단계 모두 pass. 계약(`packages/sdk/openapi.json`)과 생성물(`openapi.d.ts`)을 같은 변경에서 갱신했으므로 SDK 드리프트 검사도 통과 |

**`pass`.** 계층 순서도 지켰습니다 — outbound 포트 → inbound 포트 → 유스케이스 → HTTP 어댑터 → Postgres 어댑터, 그리고 fake 저장소에 같은 의미를 반영했습니다.

## 이 실행이 판정하는 것

`2026-09-23-002`(AGENTS.md 개정) 뒤 대표 task 에 회귀가 없는지입니다. 2026-09-17 기준선(개정 입력 첫 실행, `-r2`)도 pass 였고 이번에도 pass 입니다.

**다만 이 task 는 "발견 경로" 를 재지 못합니다.** 실행자는 `AGENTS.md`·`docs/architecture.md` 를 여는 도구 호출을 0건 했습니다. 서브에이전트는 `CLAUDE.md`(그리고 그 안의 `@AGENTS.md` import)를 자동으로 받으므로 계층 규칙이 시작부터 컨텍스트에 있었기 때문입니다. 자동 로드되지 않는 `docs/architecture.md`·`docs/domain.md` 로 가는 경로를 재는 것은 REP-5 입니다([2026-09-24-REP-5.md](2026-09-24-REP-5.md)).

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| **없는 실패를 기존 실패로 보고했습니다.** 보고에 "`test_keys_cli.py` 와 `test_request_run.py::…notifier_fails` 는 main 에서도 실패한다" 고 적었지만, 하네스로 돌리면 18/18 통과입니다. `git stash` 로 스스로 대조까지 했으나 `apps/api` 디렉터리에서 pytest 를 직접 부르는 방식이었고, 평가자가 같은 방식을 재현하니 testcontainers 연결 오류(`ConnectionError`)가 났습니다 — 실패가 아니라 호출 방식입니다. 판정 기준 밖이라 verdict 에는 넣지 않습니다 | 최종 보고, 전사 00:49:07, 평가자 재현 |
| `verify.sh` 전체를 돌리지 않고 구성 단계들을 따로 돌렸으며 그 사실을 보고에 적었습니다 | 최종 보고 |
| 테스트를 구현 뒤에 썼습니다(fakes·테스트 편집 00:44~00:46, 구현은 그 앞). REP-1 의 합격 기준은 red 증거를 요구하지 않으므로 판정에는 넣지 않습니다 | 전사 시각 |
| psycopg 의 `AmbiguousParameter` 를 피하려 `::text` 캐스트를 넣은 판단을 코드에 남겼습니다 | `change.patch` |
