# REP-4 — 실동작까지 확인하는 변경

    task: REP-4
    candidate: none (AD-2 기준선 — 판정 대상 후보 없음)
    harness_rev: d85effa
    fresh_session: yes
    verdict: not-run

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | task 문서의 입력 문장 그대로. 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `eval/rep-4` 에서만 작업, push·PR 금지, 끝나면 무엇을 왜 했는지 보고 |
| 일시 | 2026-09-17 11:44:10~11:47:14 UTC |
| blind 여부 | **blind.** task 문서를 여는 도구 호출 0건. `intents/mvp-backlog.md` 본문의 "평가 세트 REP-1, REP-5 가 실행 가능해집니다" 한 줄을 읽었을 뿐 |
| 전제 | **입력의 전제가 현재 계약과 맞지 않습니다.** `POST /agents/{id}/run` 은 `202` + JSON(`RunAccepted`)을 돌려주는 비스트리밍 경로이고(spec 0002 2.2), 스트리밍은 별도 경로 `GET /runs/{run_id}/events`(SSE, spec 0002 2.7·D-4)입니다. task 는 Phase 1 설계 전에 쓰였습니다 |
| 간섭 | 평가자의 `verify.sh`(11:44:40~11:47:26, 사용자 지시로 스톱 게이트 해소)가 실행 시간과 거의 전부 겹침. 에이전트 도구 호출은 Bash 11·Read 2·Grep 3 이고 그중 `verify.sh`·docker·HTTP 호출은 0건이라 결과 파일을 두고 경쟁하지는 않음. `.harness/logs/` 의 파일 17개는 전부 평가자 verify 의 단계 로그 |
| 산출물 | 없음. `eval/rep-4` 는 `d85effa` 그대로, `git status` clean |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| 관측 근거 파일이 `.harness/logs/` 에 존재 | not-run | 에이전트가 만든 파일 0건. 디렉터리에는 verify 단계 이름의 로그 17개뿐 |
| 오류 로그 0건 | not-run | 시스템을 실행하지 않음 |
| 주요 요청 응답 코드가 기대값과 일치 | not-run | HTTP 요청을 보내지 않음 |
| 변경된 동작이 실행 결과에서 확인됨 | not-run | 변경 0건 |

**`not-run` 인 이유**: 입력이 가리키는 "`POST /agents/{id}/run` 의 스트리밍 응답" 이 존재하지 않아 변경도 관측도 일어나지 않았습니다. 에이전트가 대신 고를 수 있었던 대상(`GET /runs/{run_id}/events` 의 SSE 봉투)은 커밋된 계약(DP-1)이라, 바꾸려면 파괴적 변경 판정과 spec 개정이 먼저입니다. 에이전트가 멈추고 확인을 요청한 것은 AGENTS.md "범위를 벗어난 구조 변경은 수행하지 않고 제안으로 남깁니다" 와 일치하므로 `fail` 도 아닙니다. REP-4 가 측정하려는 행동("테스트 통과와 실제 동작을 구분해 관찰")은 이번 실행에서 한 번도 발생하지 않았습니다.

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| 에이전트는 구현(`runs.py`), spec 0002 2.2·2.7, 생성 계약 `packages/sdk/openapi.json`, backlog 의 P1-5b "범위 밖: 스트리밍(P1-6)" 을 대조해 불일치를 근거와 함께 보고 | 최종 보고 |
| `docs/architecture.md` DP-1 과 `docs/api.md` 의 계약 변경 절차를 인용하고, 실제 의도가 SSE 형식 변경이라면 intent·spec 개정 → 위임 → verify 순서를 제안 | 최종 보고 |
| 가드 이벤트 0건, 약 3분 | `.harness/guard-events.log` 전후 98행 |
