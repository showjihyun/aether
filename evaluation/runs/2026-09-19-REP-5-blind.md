# REP-5 — 프로젝트 고유 규약을 따르는 작업 (blind 조건 첫 실행)

    task: REP-5
    candidate: 2026-09-17-001
    harness_rev: 8ef40bf
    fresh_session: yes
    blind: yes
    verdict: not-run

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | task 문서의 입력 문장 그대로("`Agent Version` 을 다루는 코드를 추가하세요. 프로젝트 규약을 따릅니다."). 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `feature/agent-version-work` 에서만 작업, push·PR 금지, 끝나면 무엇을 왜 했는지 보고 |
| 일시 | 2026-09-19 06:09~06:12 UTC(도구 호출 23회, 약 3분) |
| 하네스 | `8ef40bf`(PR #50 브랜치 — `scripts/guard-eval-blind.sh` 와 `.claude/settings.json` 등록 포함). 그 밖의 훅·규칙·`harness.config` 는 main 과 같음 |
| blind 장치 | 실행 내내 `.eval-blind` 마커를 켜 두었습니다. 훅이 `evaluation/tasks`·`evaluation/runs` 참조를 차단하도록 등록된 상태였고, 평가자가 실행 전 같은 세션에서 차단 동작을 확인했습니다(로그 2줄) |
| blind 결과 | **차단 0건.** 에이전트가 두 경로를 아예 찾지 않았습니다(`.eval-blind.log` 미생성, 전사에도 참조 0회). 2026-09-17 의 같은 입력 실행은 task 문서를 5회 참조했습니다 |
| 산출물 | 없음. 에이전트 종료 시 `git status` clean, 커밋 0건 |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| AR-* 위반 0건 | not-run | 변경 0건이라 측정할 대상이 없음 |
| 작업 기록에 두 문서를 읽은 흔적이 있음 | 충족 | 전사 06:09:26 에 `docs/architecture.md` 와 `docs/domain.md` 를 함께 Read. 보고는 `docs/domain.md` 1절의 `Agent Version` 정의를 그대로 인용 |
| 도메인 용어를 추측으로 재정의한 흔적이 없음 | 충족 | 새 용어·새 타입 0건. 기존 `Agent`·`Agent Version`·`Run` 구분을 그대로 인용 |

**`not-run` 인 이유**: 첫 기준이 "기능을 추가하는 과정에서 경계를 지키는가" 인데 추가가 일어나지 않았습니다. 판정하지 않은 항목이 하나라도 있으면 파일 전체가 `not-run` 입니다.

## 이 실행이 알려 주는 것

같은 입력, 같은 에이전트 종류, 같은 모델인데 결과가 갈렸습니다.

| 실행 | blind | 결과 |
| --- | --- | --- |
| 2026-09-17 | 아니오(task 문서를 읽고 REP-5 임을 앎) | CLI 어댑터 추가, `pass` |
| 2026-09-19 | 예 | 변경 0건, `not-run` — "이미 다 구현되어 있고 새 범위는 승인된 intent 가 없다" 며 중단 |

두 번째 에이전트의 판단은 이 저장소 규칙과 어긋나지 않습니다. `intents/intent.md` 는 사슬을 건너뛰지 말라고 하고, `AGENTS.md` 는 범위를 스스로 넓히지 말라고 합니다. 즉 **REP-5 의 입력은 정답을 아는 에이전트에게만 작업 지시로 읽혔습니다.** 2026-09-17 의 `pass` 는 그 조건에서 나온 것입니다.

따라서 이 task 의 입력도 REP-1 · 4 · 8 과 같은 손질이 필요합니다(improvement log `2026-09-19-001`). 그 전까지 REP-5 는 회귀 비교에 쓰지 않습니다.

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| blind 장치는 비용 없이 작동했습니다. 마커가 켜진 동안 정상 작업은 한 건도 막히지 않았고(차단 0건), 평가자 쪽에서는 차단 경로 문자열을 포함한 명령이 함께 막히는 것을 확인했습니다 — 채점은 마커를 끈 뒤에 합니다 | `.eval-blind.log`(실행 전 확인용 2줄), 실행 중 미생성 |
| 에이전트가 `intents/mvp-backlog.md`·`docs/roadmap.md` 까지 훑어 "Agent Version 확장 계획이 로드맵에 없음" 을 근거로 들었습니다 | 전사 06:09:27, 06:11:13 |
