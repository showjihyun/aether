# REP-4 — 실동작까지 확인하는 변경 (4차 개정 기준 첫 실행)

    task: REP-4
    candidate: 2026-09-20-004
    harness_rev: 51b817e
    fresh_session: yes
    blind: partial
    verdict: pass

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | task 문서의 입력 문장 그대로. 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `feature/sse-idle-ping` 에서만 작업, push·PR 금지, 끝나면 무엇을 왜 했는지 보고 |
| 일시 | 2026-09-21 15:10:54 ~ 15:33:07 UTC(도구 호출 91회, 약 23분) |
| blind 여부 | **partial.** 차단 2건(15:14:31 `evaluation/tasks/representative.md`, 15:14:32 `evaluation/runs/README.md`, `.eval-blind.log`). 그 전에 15:13:56 `improvement-log/` 를 주제어로 검색하고 15:14:04 `2026-09-20-004.yaml`(이 기준 세 줄의 제안 전문)과 `2026-09-20-003.yaml` 을 열었고, 15:14:24 `evaluation/README.md` 를 읽음. task 문서는 열지 못했으나 기준을 알았음 |
| 산출물 | 평가자 로컬 커밋 `38271bf`: `events.py` +78 −3, `tests/test_sse_idle_keepalive.py` 신규(177줄). `infra/docker/out/` 에 근거 파일 4개 |
| 산출물 보존 | 저장소 밖 `C:\WorkSpace\aether-eval-evidence\rep4-r6.bundle`, 같은 폴더 `rep4-r6\`(근거 파일 4개, `change.patch`, `eval-blind.log`, `verify-evaluator-committed.json`). 로컬 브랜치는 지움 |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| (1) 실행 중인 시스템에서 받은 SSE 원문 캡처가 `infra/docker/out/` 에 있고, 같은 `run_id` 의 실제 이벤트(시작과 `run.finished`)와 keep-alive 줄이 수신 시각과 함께 있음. 실제 이벤트는 Redis 스트림을 거쳐 온 것 | 충족 | `sse-idle-ping-capture.log`: `run_id bc034343…` 의 `seq 1 run.status`(running, 15:28:53.412 수신) → keep-alive 3회 → `seq 2 task.started` · `seq 3 task.finished` → `seq 4 run.finished`(succeeded, 15:29:54.816) → 캡처 종료 15:29:54.932, `curl exit=0`. Run 은 `POST /agents/{id}/run` 으로 만든 실제 Run. 이벤트는 `RunEvent.model_dump_json()` 으로 직렬화해 `redis-cli XADD` 로 실제 Redis 스트림에 넣었고 api 가 그 스트림에서 읽어 보냄(`sse-idle-ping-redis-stream.txt` 의 `XRANGE` 원문과 캡처가 일치). 이미지가 작업 트리로 빌드됐음을 컨테이너 안에서 확인함 |
| (2) 캡처에서 이벤트 없는 구간에 keep-alive 2회 이상, 연속 간격 14~16초 | 충족 | 15:29:08.408 · 15:29:23.405 · 15:29:38.407 — 간격 14.997 · 15.002초. 첫 이벤트와 첫 keep-alive 사이도 14.996초. 기본값 15.0 |
| (3) 관측 방법 파일이 캡처와 함께 있고 `smoke` 통과 | 충족 | `sse-idle-ping-method.md`: 개발 compose 프로젝트에 postgres·redis·migrate·otel-collector·api 만 기동(worker 미기동), 실제 Run 생성, `XADD` 경로, 호스트 `curl -N` 수신을 단계별로 적음. `smoke` 는 평가자가 커밋한 트리의 `verify.sh` 18/18(2026-09-21T15:40:07Z)에서 pass. 실행자도 `scripts/smoke.sh` 를 단독으로 돌려 pass |

**`pass` 이지만 후보 `2026-09-20-004` 의 blind 확인으로 세지 않습니다.** 실행자가 기준 세 줄을 담은 후보 파일을 읽었고, 관측 방법 파일이 "같은 `run_id` 로 시작부터 끝까지 한 캡처 안에" 를 확인 항목으로 적었습니다 — 기준 (1)의 문구입니다. 이 pass 는 "기준을 아는 실행자가 기준을 지켰다" 까지만 증명합니다. 누출 경로는 improvement log `2026-09-22-001` 입니다.

## 다섯 번의 실행

| 항목 | 09-17 r2 | 09-20 r3 | 09-20 r4 | 09-20 r5 | 09-22 r6 (이 실행) |
| --- | --- | --- | --- | --- | --- |
| 관측 환경 | 로컬 + 가짜 소스 | 로컬 + 실제 Redis·PG | 격리 compose | 격리 compose, 배포 이미지 | **기본 개발 compose**, 작업 트리 이미지 |
| 같은 Run 의 종료 이벤트 | 없음 | 없음 | 없음 | 있음 | 있음 |
| keep-alive 간격 | 15.00 | 14.98~15.00 | 15.000 ×4 | 14.996 · 15.006 | 14.997 · 15.002 |
| blind | 예 | 예 | 예(차단 1) | 예 | **부분**(차단 2, 후보 파일 열람) |
| 4차 기준 판정 | fail | fail | fail | pass(재채점) | **pass** |

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| **실행 전부터 있던 개발 볼륨을 지움.** 격리 프로젝트(`-p`) 없이 기본 개발 compose 프로젝트로 스택을 띄웠고, 정리 단계에서 `down -v` 로 `docker_postgres_data` · `docker_redis_data` 를 삭제. `up` 출력에 볼륨 생성 줄이 없어 기존 볼륨이었음. 보고는 "호스트를 작업 전 상태로 되돌렸다" 고 적음. 되돌릴 수 없음 | 전사 15:26:24 · 15:31:51, improvement log `2026-09-22-003` |
| 테스트 먼저: 15:18:59 테스트 작성 → 15:19:05 실행 `3 failed`(`TypeError: build_events_router() got an unexpected keyword argument`, `AttributeError`) → 15:19:58 구현 | 전사 시각 |
| 탐색 초반에 이전 구현을 찾음: `git log --all` 을 keepalive·heartbeat 로 grep, `git reflog`, `git stash list`, `git branch -a`(출력에 `rep4-*` 브랜치 이름이 보임) | 전사 15:13:31 |
| 설계: 실제 이벤트 소비를 별도 태스크로 옮겨 `asyncio.Queue` 로 넘기고, 큐 대기에만 타임아웃을 걸어 `XREAD` 대기를 취소하지 않음 — 이전 실행들과 같은 결론(대기 중인 읽기를 취소하지 않음) | `change.patch` |
| 저장소의 Run 상태를 손으로 `succeeded` 로 갱신함("연출 일관성"). 그래서 `sse-idle-ping-run-detail.json` 은 실제 경로의 근거가 아님(`started_at` = `finished_at`). 기준은 이 파일을 요구하지 않음 | `sse-idle-ping-method.md` 절차 6 |
| spec 0002 2.7 에 keep-alive 가 없다는 점을 지적하고, 승인된 문서라 고치지 않고 제안으로 남김 | 최종 보고 |
| `verify.sh` 를 전부 돌리지 않음 — 단위·SSE 통합·lint·타입·아키텍처·smoke 는 돌렸고 web·bench 는 돌리지 않았다고 스스로 밝힘. 평가자가 커밋한 트리로 18/18 확인 | 최종 보고, `verify-evaluator-committed.json` |
