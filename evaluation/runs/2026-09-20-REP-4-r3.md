# REP-4 — 실동작까지 확인하는 변경 (재설계 기준 첫 실행)

    task: REP-4
    candidate: 2026-09-20-001
    harness_rev: 4000c35
    fresh_session: yes
    blind: yes
    verdict: fail

## 실행 조건

| 항목 | 값 |
| --- | --- |
| 실행자 | Claude Code 서브에이전트 `general-purpose`, 모델 Sonnet 5. 앞선 대화 맥락 없음 |
| 입력 | 재설계된 task 문서의 입력 문장 그대로(keep-alive 15초, 실제 동작 근거 제시). 덧붙인 것은 작업 디렉터리, 로컬 브랜치 `feature/sse-keepalive` 에서만 작업, push·PR 금지, 끝나면 무엇을 왜 했는지 보고 |
| 일시 | 2026-09-20 08:2x~09:0x UTC(도구 호출 94회, 약 27분) |
| blind 여부 | **blind.** 평가 문서 참조 0건, 차단 0건 |
| 산출물 보존 | 평가자가 로컬 커밋한 뒤 저장소 밖 번들로 옮김. 변경 2 files, +245 −1: `events.py`, `tests/test_sse_heartbeat.py` 신규 |

## 합격 기준

| 합격 기준 | 관측 | 근거 |
| --- | --- | --- |
| (1) 캡처가 `infra/docker/out/` 에 있고 같은 `run_id` 의 `run.status` 와 `run.finished` 가 함께 있음 | **미충족** | 실행 뒤 그 디렉터리에 새 파일 0건(남은 것은 verify 의 smoke 산출물뿐). 전사에서 `infra/docker/out` 언급 0회. 라이브 관측은 `run.status: queued` 하나와 keep-alive 뿐이고 `run.finished` 는 발생하지 않음 |
| (2) 그 `run_id` 의 조회 응답이 종결 상태이고 파일로 남음 | **미충족** | 해당 파일 없음 |
| (3) 이벤트 없는 구간에 keep-alive 2회 이상, 간격 14~16초 | 충족(파일이 아니라 보고로) | 실제 uvicorn + testcontainers Redis·Postgres 로 프로덕션 기본값(15초)에서 측정: `t=0.06s id: 1` → `15.05s` → `30.05s` → `45.05s`, 간격 14.98·15.00·15.00 |
| (4) 관측 구간의 api 로그 발췌가 파일로 있고 `ERROR`·`Traceback` 0건 | **미충족** | 로그 발췌 파일 없음 |
| (5) `run.finished` 뒤 연결 종료 유지 | 충족(테스트로만) | 신규 단위 테스트 4건 중 하나가 keep-alive 뒤 `run.finished` 로 스트림이 끝나는 것을 확인. 라이브 캡처에는 없음 |
| (6) `smoke` 단계 통과 | 충족 | 에이전트 `verify.sh` 18/18, `smoke` pass, `bench` p95 39.6 ms |

**`fail` 인 이유**: 여섯 줄 중 세 줄이 미충족입니다. 라이브 관측 자체는 지난 실행보다 나아졌지만(실제 Redis·Postgres, 프로덕션 기본값, 실제 TCP), 근거가 파일로 남지 않아 다시 확인할 수 없고 `run.finished` 까지 가는 경로는 관측되지 않았습니다.

## 재설계가 바꾼 것과 바꾸지 못한 것

| 항목 | 2026-09-17 실행 | 2026-09-20 실행 |
| --- | --- | --- |
| 이벤트 소스 | 가짜(첫 이벤트 뒤 무한 대기) | **실제 Redis·Postgres**(testcontainers) |
| 서버 | 로컬 uvicorn | 로컬 uvicorn |
| 간격 측정 | 15.30 / 30.30 | 14.98 / 15.00 / 15.00(프로덕션 기본값) |
| 근거 파일 | 없음 | 없음(임시 스크립트는 확인 뒤 삭제) |
| `run.finished` 라이브 관측 | 없음 | 없음 |

기준을 조인 효과는 이벤트 소스가 진짜로 바뀐 데서 나타났습니다. 그러나 **근거를 파일로 남기라는 요구는 입력에 없습니다.** blind 실행자는 task 문서를 볼 수 없으므로 "근거를 함께 제시하세요" 를 보고 안 수치로 읽었고, 그 읽기는 합리적입니다. 지금 기준은 실행자가 알 수 없는 산출물 위치를 요구하고 있어, 성실한 실행도 통과할 수 없습니다.

`run.finished` 를 라이브로 보려면 worker 를 함께 띄워야 하는데, 입력에는 그 요구도 없습니다.

후속 제안은 improvement log `2026-09-20-002` 입니다.

## 그 밖의 관찰

| 관찰 | 근거 |
| --- | --- |
| 설계 판단이 정확함: `asyncio.wait_for` 로 타임아웃마다 대기를 취소하면 Redis 읽기 제너레이터가 죽는 것을 재현 스크립트로 먼저 확인하고, 대기 태스크를 유지하는 구조로 구현 | 에이전트 보고, `change.patch` |
| 계약 경계 존중: 첫 이벤트로 404 를 판정하는 사전 단계는 바이트를 보내면 계약이 깨지므로 keep-alive 대상에서 제외하고 그 사실을 보고 | 에이전트 보고 |
| 저장소 규칙 준수: `time.sleep`·`asyncio.sleep` 없이 테스트를 작성해 `tests/arch/test_no_sleep_in_tests.py`(R-11)를 통과 | 신규 테스트 파일 |
| docker compose 호출 0회 | 전사 |
