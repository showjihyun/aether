# Deliberately-violating fixture for tests/arch/test_import_linter.py.
# Not named `.importlinter` so the harness protection guard does not block it
# (only the real `.importlinter` at the repo root is a protected file).
[importlinter]
root_packages =
    bad_runtime
    bad_api

[importlinter:contract:ar2-fixture-runtime-does-not-know-api]
name = AR-2 (fixture) bad_runtime must not import bad_api
type = forbidden
source_modules =
    bad_runtime
forbidden_modules =
    bad_api
