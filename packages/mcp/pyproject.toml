[project]
name = "aether-mcp"
version = "0.0.0"
description = ""
requires-python = ">=3.12"
dependencies = [
    "mcp==2.2.0",
    "psycopg[binary]",
    "aether-policy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aether_mcp"]

[tool.uv.sources]
aether-policy = { workspace = true }
